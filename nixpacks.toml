[phases.setup]
nixPkgs = ["python311Full", "gcc", "chromium"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install greenlet==3.0.1",
    "pip install -r requirements.txt",
    "playwright install chromium --with-deps"
]

[start]
cmd = "python bot.py"
